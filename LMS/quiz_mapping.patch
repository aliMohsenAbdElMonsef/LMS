--- a/LMS/LMS.BusinessLogic/Services/CourseServices.cs
+++ b/LMS/LMS.BusinessLogic/Services/CourseServices.cs
@@ -74,6 +74,19 @@ namespace LMS.BusinessLogic.Services
                 }).ToList();
             }
 
+            if (entity.Quizzes != null)
+            {
+                dto.Quizzes = entity.Quizzes.Select(q => new LMS.BusinessLogic.DTOs.Quiz.ReadQuizDTO
+                {
+                    Id = q.Id,
+                    Title = q.Title,
+                    Description = q.Description,
+                    NumberOfQuestions = q.NumberOfQuestions,
+                    DurationMinutes = q.DurationMinutes,
+                    CourseId = q.CourseId,
+                    InstructorId = q.InstructorId
+                }).ToList();
+            }
+
             return dto;
         }
